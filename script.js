/* JavaScript */
function submitMessage() {
    const fname = document.getElementById('Name');
    const email = document.getElementById('Email');
    alert("Thank you " + fname.value + " (email: " + email.value + ") for your meassage!");
}